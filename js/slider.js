const slides=document.querySelectorAll('.slide');const dotsContainer=document.getElementById('sliderDots');let currentSlide=0;let sliderTimer;
slides.forEach((_,index)=>{const dot=document.createElement('button');dot.className=`dot ${index===0?'active':''}`;dot.setAttribute('aria-label',`Chuyển tới slide ${index+1}`);dot.addEventListener('click',()=>{showSlide(index);restartSlider()});dotsContainer.appendChild(dot)});
function showSlide(index){currentSlide=(index+slides.length)%slides.length;slides.forEach((slide,i)=>slide.classList.toggle('active',i===currentSlide));document.querySelectorAll('.dot').forEach((dot,i)=>dot.classList.toggle('active',i===currentSlide))}
function restartSlider(){clearInterval(sliderTimer);sliderTimer=setInterval(()=>showSlide(currentSlide+1),5500)}
document.getElementById('prevSlide').addEventListener('click',()=>{showSlide(currentSlide-1);restartSlider()});document.getElementById('nextSlide').addEventListener('click',()=>{showSlide(currentSlide+1);restartSlider()});restartSlider();
const countdownElement=document.getElementById('countdown');const saleEnd=Date.now()+12*60*60*1000;function updateCountdown(){const remaining=Math.max(0,saleEnd-Date.now());const h=Math.floor(remaining/3600000),m=Math.floor(remaining%3600000/60000),s=Math.floor(remaining%60000/1000);countdownElement.textContent=[h,m,s].map(n=>String(n).padStart(2,'0')).join(':')}updateCountdown();setInterval(updateCountdown,1000);