let cart=JSON.parse(localStorage.getItem('burgerbox-cart'))||[];const cartButton=document.getElementById('cartButton'),cartModal=document.getElementById('cartModal'),closeCart=document.getElementById('closeCart'),cartItems=document.getElementById('cartItems'),cartTotal=document.getElementById('cartTotal'),cartCount=document.getElementById('cartCount'),checkoutButton=document.getElementById('checkoutButton'),checkoutModal=document.getElementById('checkoutModal'),closeCheckout=document.getElementById('closeCheckout');
function saveCart(){localStorage.setItem('burgerbox-cart',JSON.stringify(cart))}function addToCart(id){const x=cart.find(i=>i.id===id);x?x.quantity++:cart.push({id,quantity:1});saveCart();updateCartUI();showToast('🛒 Đã thêm món vào giỏ hàng!')}function changeQuantity(id,d){const x=cart.find(i=>i.id===id);if(!x)return;x.quantity+=d;if(x.quantity<=0)cart=cart.filter(i=>i.id!==id);saveCart();updateCartUI()}function removeFromCart(id){cart=cart.filter(i=>i.id!==id);saveCart();updateCartUI();showToast('Đã xóa món khỏi giỏ hàng')}
function cartSubtotal(){return cart.reduce((s,i)=>{const p=products.find(x=>x.id===i.id);return s+(p?p.price*i.quantity:0)},0)}function updateCartUI(){cartCount.textContent=cart.reduce((s,i)=>s+i.quantity,0);if(!cart.length){cartItems.innerHTML='<p class="empty-cart">🛒 Giỏ hàng đang trống.<br><small>Thêm món ngon để bắt đầu nhé!</small></p>';cartTotal.textContent=formatPrice(0);return}let total=cartSubtotal();cartItems.innerHTML=cart.map(i=>{const p=products.find(x=>x.id===i.id);if(!p)return'';return `<div class="cart-item"><img src="${p.image}" alt="${p.name}"><div class="cart-item-info"><strong>${p.name}</strong><span>${formatPrice(p.price)} / món</span></div><div class="quantity"><button data-action="minus" data-id="${p.id}">−</button><b>${i.quantity}</b><button data-action="plus" data-id="${p.id}">+</button></div><button class="remove" data-action="remove" data-id="${p.id}">Xóa</button></div>`}).join('');cartTotal.textContent=formatPrice(total)}
cartItems.addEventListener('click',e=>{const b=e.target.closest('[data-action]');if(!b)return;const id=Number(b.dataset.id);if(b.dataset.action==='plus')changeQuantity(id,1);if(b.dataset.action==='minus')changeQuantity(id,-1);if(b.dataset.action==='remove')removeFromCart(id)});cartButton.addEventListener('click',()=>{cartModal.classList.remove('hidden');updateCartUI()});closeCart.onclick=()=>cartModal.classList.add('hidden');cartModal.onclick=e=>{if(e.target===cartModal)cartModal.classList.add('hidden')};checkoutButton.onclick=()=>{if(!cart.length)return showToast('🛒 Giỏ hàng đang trống!');cartModal.classList.add('hidden');checkoutModal.classList.remove('hidden')};closeCheckout.onclick=()=>checkoutModal.classList.add('hidden');checkoutModal.onclick=e=>{if(e.target===checkoutModal)checkoutModal.classList.add('hidden')};
const checkoutForm=document.getElementById('checkoutForm');checkoutForm.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('checkoutName').value.trim(),phone=document.getElementById('checkoutPhone').value.trim(),address=document.getElementById('checkoutAddress').value.trim(),msg=document.getElementById('checkoutMessage');if(!name||!phone||!address){msg.textContent='Vui lòng nhập đầy đủ thông tin nhận hàng.';msg.className='form-message error';return}if(!/^(0|\+84)[0-9]{9,10}$/.test(phone.replace(/\s/g,''))){msg.textContent='Số điện thoại không đúng định dạng.';msg.className='form-message error';return}const orders=JSON.parse(localStorage.getItem('burgerbox-orders')||'[]'),session=typeof getSession==='function'?getSession():null;orders.push({id:'BB'+String(Date.now()).slice(-6),customer:name,email:session?.email||'',userId:session?.id||null,total:cartSubtotal(),status:'Chờ xác nhận',date:new Date().toISOString().slice(0,10),items:cart.reduce((s,i)=>s+i.quantity,0),phone,address,payment:document.getElementById('paymentMethod').value,cart:cart.map(i=>({...i}))});localStorage.setItem('burgerbox-orders',JSON.stringify(orders));msg.textContent='🎉 Đặt hàng thành công! Đơn đã được chuyển tới hệ thống quản trị.';msg.className='form-message success';cart=[];saveCart();updateCartUI();setTimeout(()=>{checkoutModal.classList.add('hidden');checkoutForm.reset();msg.textContent=''},1800)});updateCartUI();